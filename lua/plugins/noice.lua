return {
  "folke/noice.nvim",
  opts = {
    cmdline = {
      -- bottom line, similar to the classic cmdline
      view = "cmdline",
    },
    -- messages = {
      -- simple popup instead of notify
    --   view = "popup",
    -- },
  },
}
